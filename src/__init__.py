"""Sleeper Cell — live dynasty draft intelligence.

Modules in this package are pure-Python and MUST NOT import Streamlit.
The Streamlit layer (app.py) sits on top and consumes these modules.
This separation is load-bearing: it lets the same logic later be wrapped
by FastAPI without rewriting.
"""

__version__ = "0.1.0-dev"
